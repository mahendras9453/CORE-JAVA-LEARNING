public class polyrule2 {
    /*
     A a1= new B();
     a1.m1();
     >> Compiler an JVM will search and executes static method logic ref var class.
      In above example , if m1() is static method , it isd searched and executed from class A
      ...>> compiler and JVM will search NSm declaration in ref var class , but executes its logic from CO class.
      In above example , if m1() is non static method , it is searched in class A and executes in class B
     */

}
